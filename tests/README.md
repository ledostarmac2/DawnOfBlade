# Tests

Automated tests are not configured yet.

Initial test targets:

- Pure C# unit tests for inventory add/remove behavior.
- Skill XP and level calculation tests.
- Data schema validation for JSON definitions.
- Save model serialization round-trip tests.

